version: "3.8"

services:
  user_postgres:
    container_name: ms_user_postgres
    image: "postgres:latest"
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - ms_user_postgres_data:/var/lib/postgresql/data

volumes:
  ms_user_postgres_data: